# Bài 20: Boosting (AdaBoost, Gradient Boosting, XGBoost)

> **Môn học:** Data Science với R  
> **Dữ liệu sử dụng:** `titanic.csv`, `wines.csv`, `german1.csv`, `medical_care.csv`  
> **Kiến thức yêu cầu:** Bài 18 — Decision Tree, Bài 19 — Bagging & Random Forest

---

## 1. Ôn lại: Bagging vs Boosting — Hai triết lý khác nhau

Ở Bài 19, Bagging giải quyết vấn đề **variance cao** của Decision Tree bằng cách xây nhiều cây **song song**, độc lập nhau, rồi lấy đa số phiếu.

**Boosting** tiếp cận hoàn toàn khác: xây các cây **tuần tự**, mỗi cây sau **tập trung sửa lỗi** của cây trước.

**Ẩn dụ để hiểu sự khác biệt:**

```
Bagging:   Hỏi 100 chuyên gia CÙNG LÚC → lấy ý kiến đa số
           (mỗi người làm việc độc lập)

Boosting:  Hỏi chuyên gia 1 → ghi nhận chỗ sai
           → Chuyên gia 2 tập trung vào chỗ sai đó → ghi nhận chỗ sai mới
           → Chuyên gia 3 tập trung vào chỗ sai mới ...
           → Kết hợp tất cả theo trọng số
```

**Điểm khác biệt cốt lõi:**

| | Bagging / Random Forest | Boosting |
|---|---|---|
| Xây dựng cây | Song song, độc lập | Tuần tự, phụ thuộc nhau |
| Mục tiêu | Giảm **Variance** | Giảm **Bias** |
| Cây thành phần | Cây sâu (full tree) | Cây nông (weak learner) |
| Dễ overfit | Khó | Có thể nếu tuning sai |
| Song song hóa | ✅ Dễ | ❌ Khó (tuần tự) |
| Hiệu năng thực tế | Tốt | **Thường tốt hơn** |

---

## 2. AdaBoost — Adaptive Boosting

### 2.1. Ý tưởng

AdaBoost (Freund & Schapire, 1997) là thuật toán Boosting đầu tiên được sử dụng rộng rãi. Ý tưởng đơn giản: **tăng trọng số** cho những quan sát bị dự đoán sai, để mô hình tiếp theo chú ý nhiều hơn vào chúng.

**Hình dung:**

```
Vòng 1: Học trên dữ liệu gốc (tất cả quan sát có trọng số bằng nhau)
        → Dự đoán sai quan sát {3, 7, 12}

Vòng 2: Tăng trọng số cho {3, 7, 12}
        → Học trên dữ liệu có trọng số mới
        → Dự đoán sai quan sát {7, 15, 21}

Vòng 3: Tăng trọng số cho {7, 15, 21}
        → Học trên dữ liệu có trọng số mới
        ...

Kết quả: Kết hợp tất cả mô hình theo trọng số α
         (mô hình đúng nhiều → α cao → ảnh hưởng nhiều hơn)
```

### 2.2. Thuật toán AdaBoost (cho phân loại nhị phân)

**Đầu vào:** Tập huấn luyện $\{(x_i, y_i)\}_{i=1}^N$, với $y_i \in \{-1, +1\}$, số vòng lặp $M$.

**Khởi tạo:** Trọng số đều nhau cho tất cả quan sát:
$$w_i^{(1)} = \frac{1}{N}, \quad i = 1, \ldots, N$$

**Lặp** $m = 1, 2, \ldots, M$:

1. Huấn luyện weak learner $h_m(x)$ trên dữ liệu có trọng số $\{w_i^{(m)}\}$.

2. Tính weighted error (lỗi có trọng số):
$$\epsilon_m = \sum_{i=1}^{N} w_i^{(m)} \cdot \mathbf{1}[h_m(x_i) \neq y_i]$$

3. Tính trọng số của mô hình $m$ (mô hình đúng nhiều → $\alpha$ lớn):
$$\alpha_m = \frac{1}{2} \ln\left(\frac{1 - \epsilon_m}{\epsilon_m}\right)$$

4. Cập nhật trọng số quan sát (sai → tăng, đúng → giảm):
$$w_i^{(m+1)} = w_i^{(m)} \cdot \exp\left(-\alpha_m \cdot y_i \cdot h_m(x_i)\right)$$
   Chuẩn hóa lại để tổng trọng số = 1.

**Dự đoán cuối cùng:**
$$H(x) = \text{sign}\left(\sum_{m=1}^{M} \alpha_m \cdot h_m(x)\right)$$

**Ví dụ trực quan với $\epsilon_m$:**

| $\epsilon_m$ | $\alpha_m$ | Ý nghĩa |
|---|---|---|
| 0.1 (rất tốt) | 1.10 | Mô hình được tin tưởng nhiều |
| 0.3 (tốt) | 0.42 | Mô hình được tin tưởng vừa |
| 0.5 (random) | 0.00 | Mô hình không đóng góp gì |
| > 0.5 (tệ) | < 0 | Mô hình bị đảo ngược |

---

## 3. Gradient Boosting (GBM)

### 3.1. Từ AdaBoost đến Gradient Boosting

AdaBoost hoạt động tốt nhưng bị giới hạn ở bài toán phân loại nhị phân. **Gradient Boosting** (Friedman, 2001) tổng quát hóa ý tưởng Boosting bằng ngôn ngữ của **Gradient Descent** — có thể áp dụng cho bất kỳ hàm mất mát nào.

**Ý tưởng cốt lõi:** Thay vì tăng trọng số quan sát sai (như AdaBoost), Gradient Boosting xây mỗi cây mới để **dự đoán phần sai sót (residual)** của mô hình hiện tại.

**Hình dung cho hồi quy:**

```
Mô hình ban đầu: F₀(x) = ȳ (trung bình)

Vòng 1:
  Residual₁ = y - F₀(x)        ← phần chưa giải thích được
  Cây₁ học để dự đoán Residual₁
  F₁(x) = F₀(x) + η × Cây₁(x) ← cập nhật mô hình (η = learning rate)

Vòng 2:
  Residual₂ = y - F₁(x)        ← residual nhỏ hơn rồi
  Cây₂ học để dự đoán Residual₂
  F₂(x) = F₁(x) + η × Cây₂(x)

...tiếp tục M vòng...

Kết quả: F_M(x) = F₀(x) + η×Cây₁(x) + η×Cây₂(x) + ... + η×Cây_M(x)
```

### 3.2. Công thức tổng quát

Tại mỗi bước $m$, ta tính **pseudo-residual** (gradient âm của loss function):

$$r_i^{(m)} = -\left[\frac{\partial L(y_i, F(x_i))}{\partial F(x_i)}\right]_{F=F_{m-1}}$$

Huấn luyện cây $h_m$ để dự đoán $r_i^{(m)}$, rồi cập nhật:

$$F_m(x) = F_{m-1}(x) + \eta \cdot h_m(x)$$

**Với các hàm loss khác nhau, pseudo-residual có dạng khác:**

| Bài toán | Loss Function | Pseudo-residual |
|---|---|---|
| Hồi quy | MSE: $\frac{1}{2}(y-F)^2$ | $y - F(x)$ (residual thông thường) |
| Phân loại | Log-loss | $y - p(x)$ (residual xác suất) |
| Robust | Absolute loss | $\text{sign}(y - F(x))$ |

### 3.3. Siêu tham số của GBM

| Tham số | Ý nghĩa | Ảnh hưởng |
|---|---|---|
| `n.trees` / `nrounds` | Số vòng lặp (số cây) | Nhiều → có thể overfit |
| `shrinkage` / `eta` | Learning rate $\eta$ | Nhỏ → chậm nhưng ổn định hơn |
| `interaction.depth` | Độ sâu mỗi cây | Sâu → phức tạp hơn |
| `n.minobsinnode` | Số obs tối thiểu tại leaf | Lớn → regularization |
| `bag.fraction` | Tỷ lệ quan sát lấy mẫu mỗi vòng | < 1 → Stochastic GBM |
| `subsample` | Như bag.fraction (XGBoost) | < 1 → giảm variance |

> 💡 **Quy tắc ngón tay cái:**  
> - Learning rate nhỏ ($\eta \leq 0.1$) + nhiều cây → thường tốt hơn learning rate lớn + ít cây.  
> - Dùng **Early Stopping** để tìm số cây tối ưu tự động.

---

## 4. XGBoost & LightGBM — Phiên bản tối ưu hóa

### 4.1. XGBoost (eXtreme Gradient Boosting)

XGBoost (Chen & Guestrin, 2016) là cải tiến của GBM với nhiều tối ưu hóa quan trọng:

**Về thuật toán:**
- Thêm **regularization** (L1 và L2) vào hàm mục tiêu để tránh overfitting.
- Dùng **second-order Taylor expansion** thay vì chỉ gradient bậc nhất → hội tụ nhanh hơn.

**Về kỹ thuật:**
- Xử lý **missing values** tự động (tìm hướng phân chia tốt nhất cho NA).
- Hỗ trợ **parallel processing** (dù các cây vẫn tuần tự, việc tìm điểm phân chia tối ưu được song song hóa).
- **Early stopping**: dừng khi validation metric không cải thiện sau K vòng.

**Hàm mục tiêu của XGBoost:**

$$\mathcal{L} = \underbrace{\sum_{i=1}^{N} L(y_i, \hat{y}_i)}_{\text{training loss}} + \underbrace{\sum_{m=1}^{M} \Omega(h_m)}_{\text{regularization}}$$

trong đó $\Omega(h) = \gamma T + \frac{1}{2}\lambda \|w\|^2$ ($T$ = số lá, $w$ = giá trị tại lá).

### 4.2. LightGBM

LightGBM (Microsoft, 2017) tiếp tục tối ưu hóa tốc độ với hai kỹ thuật chính:

- **GOSS (Gradient-based One-Side Sampling):** Giữ lại các quan sát có gradient lớn (đang học kém), lấy mẫu ngẫu nhiên các quan sát có gradient nhỏ → giảm dữ liệu cần xử lý mà không mất nhiều thông tin.

- **EFB (Exclusive Feature Bundling):** Gộp các biến thưa (sparse features) hiếm khi khác 0 cùng lúc thành một biến → giảm số biến cần xét.

- **Leaf-wise tree growth** (thay vì level-wise như XGBoost): Chọn leaf có gain lớn nhất để phân chia tiếp → cây không đều nhưng hội tụ nhanh hơn.

### 4.3. So sánh GBM, XGBoost, LightGBM

| | GBM (`gbm`) | XGBoost | LightGBM |
|---|---|---|---|
| Tốc độ | Chậm | Nhanh | **Nhanh nhất** |
| Bộ nhớ | Cao | Trung bình | **Thấp nhất** |
| Regularization | Giới hạn | ✅ L1, L2 | ✅ L1, L2 |
| Missing values | Cần xử lý thủ công | ✅ Tự động | ✅ Tự động |
| Categorical features | Cần encode | Cần encode | ✅ Hỗ trợ trực tiếp |
| Early stopping | ✅ | ✅ | ✅ |
| Phổ biến trong thực tế | Trung bình | **Rất cao** | Cao |

---

## 5. Thực hành trong R

### Cài đặt thư viện

```r
# install.packages(c("gbm", "xgboost", "lightgbm", "caret",
#                    "tidymodels", "pROC", "tidyverse", "vip"))

library(tidyverse)
library(gbm)        # Gradient Boosting Machine
library(xgboost)    # XGBoost
library(caret)      # Unified interface
library(tidymodels) # Modern workflow
library(pROC)
library(vip)
```

---

### 5.1. AdaBoost — German Credit

```r
german <- read.csv("german1.csv")
german$target <- factor(german$target, levels = c(1, 2),
                         labels = c("Good", "Bad"))

set.seed(42)
train_idx <- createDataPartition(german$target, p = 0.8, list = FALSE)
g_train   <- german[train_idx, ]
g_test    <- german[-train_idx, ]

# AdaBoost dùng caret với method = "adaboost"
# install.packages("adabag") nếu chưa có
ctrl <- trainControl(
  method          = "cv",
  number          = 5,
  classProbs      = TRUE,
  summaryFunction = twoClassSummary
)

set.seed(42)
m_ada <- train(
  target ~ .,
  data      = g_train,
  method    = "adaboost",
  trControl = ctrl,
  metric    = "ROC",
  tuneGrid  = expand.grid(
    nIter  = c(50, 100, 150, 200),  # Số vòng lặp (= số weak learner)
    method = "Adaboost.M1"
  )
)

print(m_ada$results[, c("nIter", "ROC")])
cat("Số vòng tối ưu:", m_ada$bestTune$nIter, "\n")
cat("AUC (CV):      ", round(max(m_ada$results$ROC), 4), "\n")

# Đánh giá trên test set
pred_ada  <- predict(m_ada, newdata = g_test, type = "prob")[, "Bad"]
roc_ada   <- roc(g_test$target, pred_ada,
                 levels = c("Good", "Bad"), quiet = TRUE)
cat("AUC (Test):    ", round(auc(roc_ada), 4), "\n")
```

**Quan sát trọng số thay đổi theo vòng lặp (minh họa thủ công):**

```r
# Minh họa cơ chế AdaBoost: trọng số tăng dần cho quan sát sai
library(rpart)

# Dùng biến số để đơn giản
g_num <- german %>%
  select(where(is.numeric), target) %>%
  mutate(y = ifelse(target == "Bad", 1, -1)) %>%
  select(-target)

n <- nrow(g_num)
w <- rep(1/n, n)  # Trọng số ban đầu bằng nhau

set.seed(42)
M_demo <- 5  # Chỉ demo 5 vòng

for (m in 1:M_demo) {
  # Huấn luyện cây nông (depth = 1: decision stump)
  tree_m <- rpart(y ~ . - y, data = g_num, method = "anova",
                  weights = w,
                  control = rpart.control(maxdepth = 1, cp = 0))

  pred_m  <- sign(predict(tree_m, g_num))
  correct <- (pred_m == g_num$y)

  # Weighted error
  eps_m   <- sum(w[!correct]) / sum(w)
  alpha_m <- 0.5 * log((1 - eps_m) / eps_m)

  # Cập nhật trọng số
  w <- w * exp(-alpha_m * g_num$y * pred_m)
  w <- w / sum(w)  # Chuẩn hóa

  cat(sprintf("Vòng %d | ε=%.4f | α=%.4f | w_max=%.4f | w_min=%.6f\n",
              m, eps_m, alpha_m, max(w), min(w)))
}
```

> 💡 **Quan sát:** `w_max` tăng dần theo vòng lặp — những quan sát khó (bị sai nhiều lần) nhận trọng số ngày càng lớn, buộc các mô hình sau phải chú ý nhiều hơn.

---

### 5.2. Gradient Boosting với `gbm` — Titanic

```r
titanic <- read.csv("titanic.csv", stringsAsFactors = TRUE)
titanic_clean <- titanic %>%
  select(survived, pclass, sex, age, sibsp, parch, fare) %>%
  mutate(
    survived = as.integer(survived),  # gbm cần 0/1 numeric
    sex      = as.integer(sex == "female")
  ) %>%
  drop_na()

set.seed(42)
tit_split  <- initial_split(titanic_clean, prop = 0.8, strata = survived)
tit_train  <- training(tit_split)
tit_test   <- testing(tit_split)

# Huấn luyện GBM
set.seed(42)
gbm_model <- gbm(
  survived ~ .,
  data             = tit_train,
  distribution     = "bernoulli",  # Phân loại nhị phân
  n.trees          = 500,
  shrinkage        = 0.05,         # Learning rate η
  interaction.depth = 3,           # Độ sâu mỗi cây
  n.minobsinnode   = 10,
  bag.fraction     = 0.8,          # Stochastic GBM
  cv.folds         = 5,            # Cross-validation để tìm ntree tối ưu
  verbose          = FALSE
)

# Tìm số cây tối ưu qua CV
best_ntree <- gbm.perf(gbm_model, method = "cv", plot.it = TRUE)
cat("Số cây tối ưu (CV):", best_ntree, "\n")

# Dự đoán với số cây tối ưu
pred_gbm <- predict(gbm_model, newdata = tit_test,
                    n.trees = best_ntree, type = "response")

roc_gbm <- roc(tit_test$survived, pred_gbm, quiet = TRUE)
cat("AUC (GBM):", round(auc(roc_gbm), 4), "\n")
```

**Feature Importance từ GBM:**

```r
# Relative influence của mỗi biến
imp_gbm <- summary(gbm_model, n.trees = best_ntree, plotit = FALSE)

ggplot(imp_gbm, aes(x = reorder(var, rel.inf), y = rel.inf)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(title = "Feature Importance — GBM (Titanic)",
       x = NULL, y = "Relative Influence (%)") +
  theme_minimal()
```

**Partial Dependence Plot — mối quan hệ biến với kết quả:**

```r
# Partial dependence của age với xác suất sống sót
plot(gbm_model, i.var = "age", n.trees = best_ntree,
     main = "Partial Dependence: Age vs P(survived)")

# Partial dependence của pclass
plot(gbm_model, i.var = "pclass", n.trees = best_ntree,
     main = "Partial Dependence: Pclass vs P(survived)")
```

---

### 5.3. XGBoost — German Credit

XGBoost yêu cầu dữ liệu ở dạng **matrix số**, không dùng được data.frame với biến phân loại trực tiếp.

```r
library(xgboost)

# Chuẩn bị dữ liệu: chuyển sang matrix số
# Dùng model.matrix để tự động dummy-encode biến phân loại
X_train <- model.matrix(target ~ . - 1, data = g_train)
X_test  <- model.matrix(target ~ . - 1, data = g_test)

y_train <- as.integer(g_train$target == "Bad")  # 1 = Bad, 0 = Good
y_test  <- as.integer(g_test$target  == "Bad")

# Tạo DMatrix — định dạng tối ưu của XGBoost
dtrain <- xgb.DMatrix(data = X_train, label = y_train)
dtest  <- xgb.DMatrix(data = X_test,  label = y_test)

# Tham số XGBoost
params <- list(
  objective   = "binary:logistic",  # Phân loại nhị phân
  eval_metric = "auc",
  eta         = 0.05,               # Learning rate
  max_depth   = 4,                  # Độ sâu cây
  subsample   = 0.8,                # Lấy mẫu quan sát
  colsample_bytree = 0.8,           # Lấy mẫu biến
  lambda      = 1,                  # L2 regularization
  alpha       = 0                   # L1 regularization
)

# Huấn luyện với Early Stopping
set.seed(42)
xgb_model <- xgb.train(
  params    = params,
  data      = dtrain,
  nrounds   = 500,
  watchlist = list(train = dtrain, test = dtest),
  early_stopping_rounds = 30,   # Dừng nếu AUC không tăng sau 30 vòng
  print_every_n = 50,
  verbose   = 1
)

cat("\nSố vòng tối ưu (Early Stopping):", xgb_model$best_iteration, "\n")
cat("AUC tốt nhất (validation):      ", round(xgb_model$best_score, 4), "\n")
```

**Đánh giá trên test set:**

```r
pred_xgb <- predict(xgb_model, newdata = dtest)

roc_xgb <- roc(y_test, pred_xgb, quiet = TRUE)
cat("AUC (XGBoost, test):", round(auc(roc_xgb), 4), "\n")

# Confusion matrix
pred_class_xgb <- factor(ifelse(pred_xgb >= 0.5, "Bad", "Good"),
                          levels = c("Good", "Bad"))
confusionMatrix(pred_class_xgb, g_test$target, positive = "Bad")
```

**Feature Importance từ XGBoost:**

```r
# XGBoost cung cấp 3 loại importance
imp_xgb <- xgb.importance(model = xgb_model)
print(head(imp_xgb, 10))

# Gain: tổng mức giảm loss do biến này gây ra (tương tự MDI)
# Cover: số quan sát bị ảnh hưởng bởi biến này
# Frequency: tần suất biến được chọn để phân chia

xgb.plot.importance(imp_xgb, top_n = 15,
                    main = "Feature Importance — XGBoost (German Credit)")
```

**Tuning XGBoost với `caret`:**

```r
# Chuẩn bị lại data dạng factor cho caret
g_train_c <- g_train
g_test_c  <- g_test

ctrl_xgb <- trainControl(
  method          = "cv",
  number          = 5,
  classProbs      = TRUE,
  summaryFunction = twoClassSummary,
  verboseIter     = FALSE
)

xgb_grid <- expand.grid(
  nrounds          = c(100, 200, 300),
  eta              = c(0.01, 0.05, 0.1),
  max_depth        = c(3, 4, 6),
  gamma            = 0,
  colsample_bytree = 0.8,
  min_child_weight = 1,
  subsample        = 0.8
)

set.seed(42)
m_xgb <- train(
  target ~ .,
  data      = g_train_c,
  method    = "xgbTree",
  trControl = ctrl_xgb,
  tuneGrid  = xgb_grid,
  metric    = "ROC"
)

cat("Tham số tốt nhất:\n")
print(m_xgb$bestTune)
cat("AUC tốt nhất (CV):", round(max(m_xgb$results$ROC), 4), "\n")
```

---

### 5.4. XGBoost với `tidymodels` — Wines

```r
wines <- read.csv("wines.csv", stringsAsFactors = TRUE)
wines$type <- factor(wines$type)

set.seed(42)
w_split <- initial_split(wines, prop = 0.8, strata = type)
w_train <- training(w_split)
w_test  <- testing(w_split)
w_folds <- vfold_cv(w_train, v = 5, strata = type)

# Recipe
xgb_recipe <- recipe(type ~ . - quality, data = w_train) %>%
  step_dummy(all_nominal_predictors(), one_hot = TRUE) %>%
  step_zv(all_predictors())  # Loại biến zero-variance

# Model spec
xgb_spec <- boost_tree(
  trees          = tune(),
  tree_depth     = tune(),
  learn_rate     = tune(),
  loss_reduction = tune(),
  sample_size    = tune(),
  mtry           = tune(),
  min_n          = tune()
) %>%
  set_engine("xgboost") %>%
  set_mode("classification")

# Workflow
xgb_wf <- workflow() %>%
  add_recipe(xgb_recipe) %>%
  add_model(xgb_spec)

# Latin Hypercube grid (hiệu quả hơn grid thường khi nhiều tham số)
set.seed(42)
xgb_grid_lhc <- grid_latin_hypercube(
  trees(range          = c(100, 500)),
  tree_depth(range     = c(2, 6)),
  learn_rate(range     = c(-2, -1)),   # log10 scale: 0.01 đến 0.1
  loss_reduction(range = c(-5, 0)),
  sample_size          = sample_prop(range = c(0.6, 1.0)),
  mtry(range           = c(3, 8)),
  min_n(range          = c(2, 20)),
  size = 20  # 20 điểm thử
)

# Tune
set.seed(42)
xgb_tune <- tune_grid(
  xgb_wf,
  resamples = w_folds,
  grid      = xgb_grid_lhc,
  metrics   = metric_set(roc_auc, accuracy),
  control   = control_grid(verbose = FALSE)
)

# Kết quả tốt nhất
show_best(xgb_tune, metric = "roc_auc", n = 5)

# Vẽ kết quả
autoplot(xgb_tune) +
  labs(title = "XGBoost Tuning — Wines (tidymodels)")

# Finalize
best_xgb  <- select_best(xgb_tune, metric = "roc_auc")
final_xgb <- last_fit(finalize_workflow(xgb_wf, best_xgb), w_split)

cat("\n--- Kết quả trên Test Set (Wines) ---\n")
collect_metrics(final_xgb)

# ROC Curve
collect_predictions(final_xgb) %>%
  roc_curve(truth = type, .pred_red) %>%
  autoplot() +
  labs(title = "ROC Curve — XGBoost (Wines)")
```

---

### 5.5. Early Stopping — Tránh Overfitting

Early Stopping là kỹ thuật quan trọng nhất để kiểm soát overfitting trong Boosting: theo dõi performance trên validation set và **dừng sớm** khi không còn cải thiện.

```r
# Minh họa overfitting với GBM khi không có early stopping
set.seed(42)
gbm_overfit <- gbm(
  survived ~ .,
  data              = tit_train,
  distribution      = "bernoulli",
  n.trees           = 1000,        # Nhiều cây, không early stopping
  shrinkage         = 0.1,         # Learning rate lớn hơn
  interaction.depth = 5,           # Cây sâu hơn
  bag.fraction      = 1.0,         # Không stochastic
  cv.folds          = 0,           # Không CV
  verbose           = FALSE
)

# Tính AUC train và test theo từng số cây
ntree_seq   <- seq(10, 1000, by = 10)
auc_train_v <- numeric(length(ntree_seq))
auc_test_v  <- numeric(length(ntree_seq))

for (i in seq_along(ntree_seq)) {
  p_tr <- predict(gbm_overfit, tit_train,
                  n.trees = ntree_seq[i], type = "response")
  p_te <- predict(gbm_overfit, tit_test,
                  n.trees = ntree_seq[i], type = "response")

  auc_train_v[i] <- auc(roc(tit_train$survived, p_tr, quiet = TRUE))
  auc_test_v[i]  <- auc(roc(tit_test$survived,  p_te, quiet = TRUE))
}

# Vẽ đồ thị
data.frame(ntree = ntree_seq,
           Train = auc_train_v,
           Test  = auc_test_v) %>%
  pivot_longer(-ntree, names_to = "Set", values_to = "AUC") %>%
  ggplot(aes(x = ntree, y = AUC, color = Set)) +
  geom_line(linewidth = 0.9) +
  geom_vline(xintercept = ntree_seq[which.max(auc_test_v)],
             linetype = "dashed", color = "gray40") +
  scale_color_manual(values = c("Train" = "steelblue", "Test" = "tomato")) +
  labs(title    = "Overfitting trong GBM — Titanic",
       subtitle  = "Đường đứt: số cây tối ưu cho test AUC",
       x = "Số cây (n.trees)", y = "AUC") +
  theme_minimal()

best_ntree_manual <- ntree_seq[which.max(auc_test_v)]
cat("Số cây cho test AUC cao nhất:", best_ntree_manual, "\n")
cat("Train AUC tại điểm đó:       ", round(auc_train_v[which.max(auc_test_v)], 4), "\n")
cat("Test AUC cao nhất:            ", round(max(auc_test_v), 4), "\n")
```

> ⚠️ **Nhận xét:** Train AUC liên tục tăng đến 1.0, trong khi Test AUC tăng rồi giảm — đây là dấu hiệu overfitting rõ ràng. Early Stopping giải quyết vấn đề này bằng cách dừng đúng lúc.

---

### 5.6. Medical Care — So sánh toàn diện

```r
medical <- read.csv("medical_care.csv", stringsAsFactors = TRUE)

medical_clean <- medical %>%
  select(UCURNINS, UMARSTAT, USATMED, REGION, FHOSP, FDENT, FEMER,
         FDOCT, UIMMSTAT, UAGE, U_FTPT, U_WKSLY, UBRACE, GENDER, UEDUC3) %>%
  drop_na() %>%
  mutate(UCURNINS = factor(UCURNINS))

set.seed(42)
med_split <- initial_split(medical_clean, prop = 0.8, strata = UCURNINS)
med_train <- training(med_split)
med_test  <- testing(med_split)

# XGBoost trên dữ liệu lớn
xgb_recipe_med <- recipe(UCURNINS ~ ., data = med_train) %>%
  step_dummy(all_nominal_predictors(), one_hot = TRUE) %>%
  step_zv(all_predictors())

xgb_spec_med <- boost_tree(
  trees      = 300,
  tree_depth = 4,
  learn_rate = 0.05,
  sample_size = 0.8,
  mtry       = 0.8     # colsample_bytree
) %>%
  set_engine("xgboost") %>%
  set_mode("classification")

xgb_wf_med <- workflow() %>%
  add_recipe(xgb_recipe_med) %>%
  add_model(xgb_spec_med)

set.seed(42)
xgb_fit_med <- fit(xgb_wf_med, data = med_train)

pred_xgb_med <- predict(xgb_fit_med, med_test, type = "prob")$.pred_Yes
roc_xgb_med  <- roc(med_test$UCURNINS, pred_xgb_med,
                    levels = c("No","Yes"), quiet = TRUE)

cat("AUC — XGBoost (Medical):", round(auc(roc_xgb_med), 4), "\n")
```

---

### 5.7. So sánh tất cả mô hình — German Credit

```r
# Dùng caret để so sánh trên cùng CV folds
ctrl_all <- trainControl(
  method          = "cv",
  number          = 5,
  classProbs      = TRUE,
  summaryFunction = twoClassSummary,
  savePredictions = "final",
  seeds           = lapply(1:6, function(x) 1:20)  # Reproducibility
)

set.seed(42)

# Decision Tree (Bài 18)
m_tree <- train(target ~ ., g_train, method = "rpart",
                trControl = ctrl_all, metric = "ROC",
                tuneGrid = expand.grid(cp = c(0.001, 0.01, 0.05)))

# Random Forest (Bài 19)
m_rf   <- train(target ~ ., g_train, method = "rf",
                trControl = ctrl_all, metric = "ROC",
                tuneGrid = expand.grid(mtry = c(3, 5, 7)),
                ntree = 200)

# GBM
m_gbm  <- train(target ~ ., g_train, method = "gbm",
                trControl = ctrl_all, metric = "ROC",
                tuneGrid  = expand.grid(
                  n.trees           = c(100, 200),
                  interaction.depth = c(2, 3),
                  shrinkage         = c(0.05, 0.1),
                  n.minobsinnode    = 10
                ),
                verbose = FALSE)

# XGBoost
m_xgb  <- train(target ~ ., g_train, method = "xgbTree",
                trControl = ctrl_all, metric = "ROC",
                tuneGrid  = expand.grid(
                  nrounds          = c(100, 200),
                  eta              = c(0.05, 0.1),
                  max_depth        = c(3, 4),
                  gamma            = 0,
                  colsample_bytree = 0.8,
                  min_child_weight = 1,
                  subsample        = 0.8
                ),
                verbose = FALSE)

# Logistic Regression (baseline từ Bài 17)
m_lr   <- train(target ~ ., g_train, method = "glm",
                family = "binomial",
                trControl = ctrl_all, metric = "ROC")

# Tổng hợp kết quả
results_all <- resamples(list(
  LogisticReg  = m_lr,
  DecisionTree = m_tree,
  RandomForest = m_rf,
  GBM          = m_gbm,
  XGBoost      = m_xgb
))

summary(results_all, metric = "ROC")

# Dotplot so sánh
dotplot(results_all, metric = "ROC",
        main = "So sánh AUC (5-Fold CV) — German Credit\nBài 17–20")

# Boxplot chi tiết
bwplot(results_all, metric = "ROC",
       main = "Phân phối AUC (5-Fold CV)")

# Đánh giá trên test set
get_test_auc <- function(model, test_data, truth, pos) {
  p   <- predict(model, newdata = test_data, type = "prob")[, pos]
  lvl <- levels(test_data[[truth]])
  round(auc(roc(test_data[[truth]], p, levels = lvl, quiet = TRUE)), 4)
}

cat("\n===== AUC trên TEST SET — German Credit =====\n")
cat("Logistic Regression (Bài 17):", get_test_auc(m_lr,   g_test, "target", "Bad"), "\n")
cat("Decision Tree       (Bài 18):", get_test_auc(m_tree, g_test, "target", "Bad"), "\n")
cat("Random Forest       (Bài 19):", get_test_auc(m_rf,   g_test, "target", "Bad"), "\n")
cat("GBM                 (Bài 20):", get_test_auc(m_gbm,  g_test, "target", "Bad"), "\n")
cat("XGBoost             (Bài 20):", get_test_auc(m_xgb,  g_test, "target", "Bad"), "\n")
```

---

### 5.8. So sánh tất cả mô hình — Bảng tổng kết đa dataset

```r
# Hàm tiện lợi: train nhanh và lấy CV AUC
quick_auc_cv <- function(method, train_data, target_col, tuneGrid = NULL,
                          ctrl = ctrl_all, ...) {
  fml <- as.formula(paste(target_col, "~ ."))
  args <- list(form = fml, data = train_data, method = method,
               trControl = ctrl, metric = "ROC")
  if (!is.null(tuneGrid)) args$tuneGrid <- tuneGrid
  args <- c(args, list(...))
  m <- do.call(train, args)
  round(max(m$results$ROC, na.rm = TRUE), 4)
}

# Titanic
titanic_f <- titanic %>%
  select(survived, pclass, sex, age, sibsp, parch, fare) %>%
  mutate(survived = factor(survived, levels = c(0,1),
                           labels = c("No","Yes"))) %>%
  drop_na()
set.seed(42)
tit_tr <- titanic_f[createDataPartition(titanic_f$survived, p=0.8)[[1]], ]

summary_table <- tibble(
  Model = c("Logistic Regression", "Decision Tree",
            "Random Forest", "GBM", "XGBoost"),
  German_Credit = c(
    get_test_auc(m_lr,   g_test, "target", "Bad"),
    get_test_auc(m_tree, g_test, "target", "Bad"),
    get_test_auc(m_rf,   g_test, "target", "Bad"),
    get_test_auc(m_gbm,  g_test, "target", "Bad"),
    get_test_auc(m_xgb,  g_test, "target", "Bad")
  )
)

print(summary_table)
```

---

## 6. Hướng dẫn Tuning thực tế

Tuning Boosting có nhiều tham số hơn Random Forest. Dưới đây là **chiến lược theo bước** cho người mới:

### Bước 1: Cố định learning rate thấp, tìm số cây

```r
# Dùng learning rate = 0.05, tìm n.trees qua CV
gbm_step1 <- gbm(target_num ~ ., data = g_train_num,
                  distribution = "bernoulli",
                  n.trees = 1000, shrinkage = 0.05,
                  interaction.depth = 4, cv.folds = 5,
                  verbose = FALSE)
best_n <- gbm.perf(gbm_step1, method = "cv")
cat("Số cây tối ưu với eta=0.05:", best_n, "\n")
```

### Bước 2: Tune depth và min obs

```r
# Thử các giá trị depth với số cây đã tìm
ctrl_step2 <- trainControl(method = "cv", number = 5,
                            classProbs = TRUE,
                            summaryFunction = twoClassSummary)
grid_step2 <- expand.grid(
  n.trees           = best_n,         # Cố định từ bước 1
  interaction.depth = c(2, 3, 4, 5),
  shrinkage         = 0.05,
  n.minobsinnode    = c(5, 10, 20)
)
# ... train như bình thường
```

### Bước 3: Giảm learning rate, tăng số cây tương ứng

```r
# Nếu eta giảm 2x → n.trees tăng 2x để giữ nguyên "tổng bước học"
# eta = 0.05, n.trees = 200 ≈ eta = 0.025, n.trees = 400
```

### Bước 4: Thêm stochastic sampling

```r
# Thêm bag.fraction và colsample để giảm variance
grid_step4 <- expand.grid(
  n.trees           = best_n * 2,
  interaction.depth = 3,
  shrinkage         = 0.025,
  n.minobsinnode    = 10,
  bag.fraction      = c(0.7, 0.8, 1.0)  # Stochastic
)
```

> 💡 **Lưu ý thực tế:** Với `xgboost` qua `tidymodels`, dùng `grid_latin_hypercube()` (như mục 5.4) thay vì grid thông thường để khám phá không gian tham số hiệu quả hơn khi có nhiều tham số.

---

## 7. So sánh tổng thể Bài 17–20

```
Độ phức tạp tăng dần →
┌─────────────────┬──────────────┬──────────────┬──────────────┬──────────────┐
│ Logistic Reg    │ Decision Tree│ Random Forest│    GBM       │   XGBoost    │
│   (Bài 17)      │   (Bài 18)   │   (Bài 19)   │  (Bài 20)    │  (Bài 20)    │
├─────────────────┼──────────────┼──────────────┼──────────────┼──────────────┤
│ Tuyến tính      │ Phi tuyến    │ Phi tuyến    │ Phi tuyến    │ Phi tuyến    │
│ Không ensemble  │ Không        │ Bagging      │ Boosting     │ Boosting     │
│ Dễ giải thích ✅│ Dễ ✅        │ Khó ❌       │ Khó ❌       │ Khó ❌       │
│ Cần chuẩn hóa  │ Không cần    │ Không cần    │ Không cần    │ Cần encode   │
│ Hiệu năng: TB  │ Thấp         │ Tốt          │ Tốt hơn      │ Thường tốt   │
│                 │              │              │              │ nhất ✅      │
└─────────────────┴──────────────┴──────────────┴──────────────┴──────────────┘
```

**Khi nào chọn gì:**

| Tình huống | Khuyến nghị |
|---|---|
| Cần giải thích cho non-technical | Logistic Regression hoặc Decision Tree |
| Dữ liệu nhỏ, nhiều biến phân loại | Random Forest |
| Dữ liệu lớn, cần accuracy cao | XGBoost / LightGBM |
| Ít thời gian tuning | Random Forest (OOB tự đánh giá) |
| Kaggle / Competition | XGBoost / LightGBM |
| Dữ liệu chuỗi thời gian có xu hướng | GBM / XGBoost |

---

## 8. Bài tập

### Bài tập 1 — German Credit (`german1.csv`)

1. Huấn luyện GBM với `cv.folds = 5`. Dùng `gbm.perf()` để tìm số cây tối ưu. AUC trên test set là bao nhiêu?
2. Vẽ đồ thị Training vs Validation error theo số cây. Khoảng nào bắt đầu overfitting?
3. Huấn luyện XGBoost với early stopping (30 vòng). So sánh số cây tối ưu với GBM ở câu 1.
4. Dùng `caret` để so sánh 5 mô hình: Logistic Regression, Decision Tree, Random Forest, GBM, XGBoost bằng `resamples()` và `dotplot()`. Mô hình nào tốt nhất? Sự cải thiện có đáng kể không?
5. *(Nâng cao)* So sánh Feature Importance của Random Forest (MDI), GBM (Relative Influence) và XGBoost (Gain). Ba mô hình có đồng thuận về top 5 biến không?

### Bài tập 2 — Titanic (`titanic.csv`)

1. Huấn luyện GBM với `bag.fraction = 0.8` (Stochastic GBM). So sánh AUC với GBM không stochastic (`bag.fraction = 1.0`). Cái nào ổn định hơn khi chạy nhiều lần?
2. Dùng `tidymodels` để tune XGBoost với `grid_latin_hypercube()` (20 điểm). Tham số nào ảnh hưởng nhiều nhất đến AUC?
3. Vẽ Partial Dependence Plot cho `age` và `pclass` từ GBM. Xu hướng có phù hợp với lịch sử Titanic không?

### Bài tập 3 — Wines (`wines.csv`)

1. Dùng XGBoost (tidymodels) để phân loại `type` (red/white). So sánh AUC với Random Forest từ Bài 19.
2. Dùng GBM để dự đoán `quality` (regression, `distribution = "gaussian"`). So sánh RMSE với Regression Tree và Random Forest.
3. *(Nâng cao)* Thử `distribution = "huberized"` trong GBM — đây là loss function bền vững với outlier. RMSE có cải thiện so với Gaussian không?

### Bài tập 4 — Medical Care (`medical_care.csv`)

1. Huấn luyện XGBoost trên toàn bộ dữ liệu (35,000+ quan sát). So sánh AUC với: Logistic Regression (Bài 17), Decision Tree (Bài 18), Random Forest (Bài 19).
2. Đo thời gian huấn luyện của Random Forest (`ranger`) và XGBoost trên cùng dataset. Mô hình nào nhanh hơn?
3. Nhìn vào Feature Importance từ XGBoost: kết quả có đồng thuận với Random Forest từ Bài 19 không? Biến nào quan trọng nhất?

---

## 9. Tài liệu tham khảo

- **AdaBoost:** Freund, Y., & Schapire, R. E. (1997). A decision-theoretic generalization of on-line learning. *Journal of Computer and System Sciences*, 55(1), 119–139.
- **Gradient Boosting:** Friedman, J. H. (2001). Greedy function approximation: A gradient boosting machine. *Annals of Statistics*, 29(5), 1189–1232.
- **XGBoost:** Chen, T., & Guestrin, C. (2016). XGBoost: A scalable tree boosting system. *KDD 2016*.
- **LightGBM:** Ke, G. et al. (2017). LightGBM: A highly efficient gradient boosting decision tree. *NeurIPS 2017*.
- **gbm package:** <https://cran.r-project.org/package=gbm>
- **xgboost package:** <https://xgboost.readthedocs.io/>
- **tidymodels:** <https://www.tidymodels.org/>
- James, G. et al. (2021). *An Introduction to Statistical Learning* (2nd ed.). Springer. Ch. 8.2: Boosting.

---

*Bài giảng được soạn cho môn Data Science với R — Bài 20: Boosting*  
*Tiếp theo: Bài 21 — Stacking & Ensemble tổng quát*
