# Bài 21: Stacking & Ensemble Tổng Quát

> **Môn học:** Data Science với R  
> **Dữ liệu sử dụng:** `titanic.csv`, `wines.csv`, `german1.csv`, `medical_care.csv`  
> **Kiến thức yêu cầu:** Bài 18 — Decision Tree, Bài 19 — Random Forest, Bài 20 — Boosting

---

## 1. Nhìn lại hành trình Bài 17–20

Trong các bài trước, chúng ta đã học ba cách kết hợp mô hình:

```
Bagging (Bài 19):  Nhiều cây SONG SONG trên bootstrap samples → Majority vote
                   Mục tiêu: Giảm Variance

Boosting (Bài 20): Nhiều cây TUẦN TỰ, mỗi cây sửa lỗi cây trước
                   Mục tiêu: Giảm Bias

Stacking (Bài 21): Nhiều MÔ HÌNH KHÁC LOẠI song song → Một meta-model học cách kết hợp
                   Mục tiêu: Tận dụng điểm mạnh của từng loại mô hình
```

**Điểm khác biệt quan trọng nhất của Stacking:**

- Bagging và Boosting kết hợp **cùng loại mô hình** (thường là Decision Tree).
- Stacking kết hợp **các mô hình khác loại nhau** — Logistic Regression, Random Forest, XGBoost, KNN, SVM... — rồi dùng một **meta-model** để học cách "tin tưởng" mỗi mô hình bao nhiêu.

---

## 2. Stacking — Ý tưởng cốt lõi

### 2.1. Ẩn dụ dễ hiểu

Hãy tưởng tượng một cuộc thi dự đoán kết quả bóng đá:

```
Chuyên gia 1 (Nhà thống kê):   Phân tích số liệu lịch sử → Dự đoán A
Chuyên gia 2 (Cựu cầu thủ):    Nhận xét về phong độ đội → Dự đoán B
Chuyên gia 3 (Nhà tâm lý):     Đánh giá tinh thần đội   → Dự đoán C

Trọng tài tổng hợp (Meta-model): Học xem mỗi chuyên gia
                                  giỏi ở tình huống nào
                                  → Dự đoán cuối cùng
```

Mỗi "chuyên gia" giỏi một khía cạnh khác nhau. Meta-model không đơn giản lấy trung bình — nó **học được** khi nào nên tin Chuyên gia 1 hơn Chuyên gia 2.

### 2.2. Cấu trúc hai tầng

```
                    DỮ LIỆU GỐC
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
    Mô hình 1      Mô hình 2      Mô hình 3     ← Tầng 1 (Base Learners)
  (Logistic Reg) (Random Forest) (XGBoost)
          │              │              │
          └──────────────┼──────────────┘
                         │
                  [p1, p2, p3]                  ← Dự đoán từ tầng 1
                         │
                    Meta-model                  ← Tầng 2 (Meta-Learner)
               (Logistic Regression)
                         │
                  Dự đoán cuối cùng
```

**Tầng 1 — Base Learners:** Các mô hình đa dạng, huấn luyện độc lập trên training data.

**Tầng 2 — Meta-Learner:** Nhận **đầu ra của tầng 1** làm đầu vào, học cách kết hợp tối ưu.

### 2.3. Vấn đề Information Leakage

Nếu huấn luyện base learners trên training data, rồi dùng dự đoán của chúng trên **cùng** training data đó để huấn luyện meta-model → **Information leakage** (rò rỉ thông tin)!

Base learners đã "nhìn thấy" training data → dự đoán trên đó quá tốt một cách giả tạo → meta-model học sai.

**Giải pháp:** Dùng **Out-of-Fold predictions** — dự đoán của mỗi base learner trên phần dữ liệu mà nó **chưa từng thấy**.

---

## 3. Stacking với Out-of-Fold Predictions

### 3.1. Quy trình đầy đủ

```
Training Data (N quan sát)
         │
    Chia thành K folds
         │
    Với mỗi fold k = 1..K:
    ┌────────────────────────────────────┐
    │ Train base learners trên K-1 folds │
    │ Dự đoán trên fold k (OOF pred)     │
    └────────────────────────────────────┘
         │
    Sau K folds: mỗi quan sát có đúng 1 OOF prediction
    từ mỗi base learner (vì nó chỉ được dự đoán khi
    nó KHÔNG nằm trong training set của base learner đó)
         │
    Tạo meta-features matrix: [OOF_pred_1, OOF_pred_2, ..., OOF_pred_M]
         │
    Huấn luyện meta-model trên meta-features + y_true
         │
    ─────────────────────────────────────────
    Để dự đoán trên TEST SET:
    - Huấn luyện lại base learners trên TOÀN BỘ training data
    - Lấy dự đoán của chúng trên test set
    - Đưa vào meta-model → Kết quả cuối
```

**Tại sao OOF tránh được leakage?**

Khi base learner dự đoán quan sát $i$, nó chưa từng thấy quan sát $i$ trong quá trình huấn luyện → dự đoán này thực sự là "out-of-sample" → meta-model học trên dữ liệu sạch.

### 3.2. Công thức

Với K-fold CV và M base learners:

**Tạo meta-features cho quan sát $i$ (thuộc fold $k$):**

$$\tilde{x}_i = \left[ \hat{p}_1^{(-k)}(x_i),\ \hat{p}_2^{(-k)}(x_i),\ \ldots,\ \hat{p}_M^{(-k)}(x_i) \right]$$

trong đó $\hat{p}_m^{(-k)}$ là dự đoán của base learner $m$ huấn luyện trên tất cả fold trừ fold $k$.

**Huấn luyện meta-model:**

$$\hat{y} = g\left(\tilde{x}\right) = g\left(\hat{p}_1, \hat{p}_2, \ldots, \hat{p}_M\right)$$

---

## 4. Blending — Phiên bản đơn giản hơn

**Blending** (hay Holdout Stacking) là cách đơn giản hơn OOF Stacking: thay vì dùng K-fold, chỉ dùng **một lần chia holdout**.

```
Training Data
     │
     ├── 70% → Huấn luyện Base Learners
     └── 30% (Blend set) → Base Learners dự đoán
                               → Dùng kết quả này huấn luyện Meta-Model

Test Data → Base Learners (đã train trên 70%) → Meta-Model → Kết quả
```

**So sánh OOF Stacking vs Blending:**

| | OOF Stacking | Blending |
|---|---|---|
| Dữ liệu train meta-model | Toàn bộ training (OOF preds) | Chỉ 30% (blend set) |
| Thông tin dùng | Nhiều hơn | Ít hơn |
| Tránh leakage | ✅ Tốt | ✅ Tốt |
| Tính toán | Phức tạp hơn | Đơn giản hơn |
| Variance của kết quả | Thấp hơn | Cao hơn |
| Phù hợp | Dữ liệu vừa/lớn | Dữ liệu nhỏ, demo nhanh |

---

## 5. So sánh ba phương pháp Ensemble

```
                BAGGING          BOOSTING         STACKING
                (Bài 19)         (Bài 20)         (Bài 21)
               ─────────        ─────────        ─────────
Mô hình        Cùng loại        Cùng loại        Khác loại
thành phần     (thường DT)      (thường DT)      (LR+RF+XGB...)

Xây dựng       Song song        Tuần tự          Song song

Kết hợp        Majority vote    Weighted sum     Meta-model học
               / Average        (tự động)        cách kết hợp

Mục tiêu       Giảm Variance    Giảm Bias        Tận dụng điểm
                                                 mạnh từng model

Nguy cơ        Khó overfit      Có thể overfit   Overfit nếu
overfit                         nếu tuning sai   không dùng OOF

Độ phức tạp    Thấp             Trung bình       Cao nhất
triển khai

Hiệu năng      Tốt              Thường tốt hơn   Thường tốt nhất
thực tế                         Bagging          (nếu base learners
                                                 đủ đa dạng)
```

**Khi nào Stacking không hiệu quả?**

- Base learners quá giống nhau → meta-model không học được gì thêm.
- Dữ liệu quá nhỏ → OOF predictions không đáng tin cậy.
- Khi một mô hình đã rất tốt → stacking chỉ cải thiện nhỏ nhưng tốn công.

---

## 6. Thực hành trong R

### Cài đặt thư viện

```r
# install.packages(c("stacks", "tidymodels", "caret", "caretEnsemble",
#                    "pROC", "tidyverse", "ranger", "xgboost", "kknn"))

library(tidyverse)
library(tidymodels)
library(stacks)       # Stacking với tidymodels
library(caret)
library(caretEnsemble)# Stacking với caret
library(pROC)
```

---

### 6.1. Blending tự tay — Hiểu rõ cơ chế

Trước khi dùng package, ta tự xây Blending để thấy rõ từng bước:

```r
titanic <- read.csv("titanic.csv", stringsAsFactors = TRUE)
titanic_clean <- titanic %>%
  select(survived, pclass, sex, age, sibsp, parch, fare) %>%
  mutate(survived = factor(survived, levels = c(0,1),
                           labels = c("No","Yes"))) %>%
  drop_na()

set.seed(42)
n   <- nrow(titanic_clean)
idx <- sample(1:n, n)

# Chia 3 phần: 60% train base, 20% blend, 20% test
train_end <- floor(0.6 * n)
blend_end <- floor(0.8 * n)

base_train  <- titanic_clean[idx[1:train_end],        ]
blend_set   <- titanic_clean[idx[(train_end+1):blend_end], ]
test_set    <- titanic_clean[idx[(blend_end+1):n],    ]

cat("Base train:", nrow(base_train),
    "| Blend:", nrow(blend_set),
    "| Test:", nrow(test_set), "\n")
```

**Bước 1: Huấn luyện base learners trên `base_train`**

```r
library(rpart)
library(ranger)
library(xgboost)

# Base learner 1: Logistic Regression
bl_lr <- glm(survived ~ ., data = base_train, family = binomial())

# Base learner 2: Decision Tree
bl_tree <- rpart(survived ~ ., data = base_train, method = "class",
                 control = rpart.control(cp = 0.01))

# Base learner 3: Random Forest
bl_rf <- ranger(survived ~ ., data = base_train,
                num.trees = 200, probability = TRUE,
                verbose = FALSE)

# Base learner 4: XGBoost (cần matrix)
X_base <- model.matrix(survived ~ . - 1, data = base_train)
y_base <- as.integer(base_train$survived == "Yes")
dtrain_base <- xgb.DMatrix(X_base, label = y_base)

bl_xgb <- xgb.train(
  params = list(objective = "binary:logistic", eta = 0.05,
                max_depth = 3, eval_metric = "auc"),
  data = dtrain_base, nrounds = 100, verbose = 0
)
```

**Bước 2: Dự đoán trên `blend_set` → tạo meta-features**

```r
# Dự đoán xác suất "Yes" từ mỗi base learner
p_lr_blend <- predict(bl_lr, newdata = blend_set, type = "response")

p_tree_blend <- predict(bl_tree, newdata = blend_set,
                        type = "prob")[, "Yes"]

p_rf_blend <- predict(bl_rf, data = blend_set)$predictions[, "Yes"]

X_blend <- model.matrix(survived ~ . - 1, data = blend_set)
p_xgb_blend <- predict(bl_xgb,
                        xgb.DMatrix(X_blend))

# Tạo meta-features matrix
meta_train <- data.frame(
  p_lr   = p_lr_blend,
  p_tree = p_tree_blend,
  p_rf   = p_rf_blend,
  p_xgb  = p_xgb_blend,
  y      = blend_set$survived
)

cat("Tương quan giữa các base learner:\n")
print(round(cor(meta_train[, 1:4]), 3))
```

> 💡 **Kiểm tra tương quan:** Muốn Stacking hiệu quả, các base learners nên có tương quan không quá cao (< 0.95). Nếu tương quan cao → các mô hình quá giống nhau → meta-model không học thêm được gì.

**Bước 3: Huấn luyện meta-model trên `blend_set`**

```r
# Meta-model: Logistic Regression đơn giản
meta_model <- glm(y ~ p_lr + p_tree + p_rf + p_xgb,
                  data = meta_train, family = binomial())

cat("\n--- Hệ số Meta-Model ---\n")
print(round(coef(meta_model), 4))
cat("\n(Hệ số dương lớn → base learner đó được 'tin tưởng' nhiều hơn)\n")
```

**Bước 4: Đánh giá trên `test_set`**

```r
# Dự đoán base learners trên test set
p_lr_test   <- predict(bl_lr,   newdata = test_set, type = "response")
p_tree_test <- predict(bl_tree, newdata = test_set, type = "prob")[, "Yes"]
p_rf_test   <- predict(bl_rf,   data = test_set)$predictions[, "Yes"]

X_test_mat  <- model.matrix(survived ~ . - 1, data = test_set)
p_xgb_test  <- predict(bl_xgb, xgb.DMatrix(X_test_mat))

meta_test <- data.frame(
  p_lr   = p_lr_test,
  p_tree = p_tree_test,
  p_rf   = p_rf_test,
  p_xgb  = p_xgb_test
)

# Dự đoán cuối từ meta-model
p_stack_test <- predict(meta_model, newdata = meta_test,
                         type = "response")

# Tính AUC của từng mô hình và stacking
get_auc <- function(prob, truth) {
  round(auc(roc(truth, prob, quiet = TRUE)), 4)
}

cat("\n===== AUC trên Test Set (Titanic) =====\n")
cat("Logistic Regression:", get_auc(p_lr_test,    test_set$survived), "\n")
cat("Decision Tree:      ", get_auc(p_tree_test,  test_set$survived), "\n")
cat("Random Forest:      ", get_auc(p_rf_test,    test_set$survived), "\n")
cat("XGBoost:            ", get_auc(p_xgb_test,   test_set$survived), "\n")
cat("----------------------------------\n")
cat("Blending (Stack):   ", get_auc(p_stack_test, test_set$survived), "\n")
```

---

### 6.2. OOF Stacking tự tay — German Credit

OOF Stacking phức tạp hơn nhưng sử dụng dữ liệu training hiệu quả hơn Blending:

```r
german <- read.csv("german1.csv")
german$target <- factor(german$target, levels = c(1,2),
                         labels = c("Good","Bad"))

set.seed(42)
g_split <- initial_split(german, prop = 0.8, strata = target)
g_train <- training(g_split)
g_test  <- testing(g_split)

K <- 5  # Số folds
n_train <- nrow(g_train)
set.seed(42)
fold_id <- sample(rep(1:K, length.out = n_train))

# Ma trận lưu OOF predictions
oof_preds <- matrix(NA, nrow = n_train, ncol = 3,
                    dimnames = list(NULL, c("p_lr","p_rf","p_xgb")))

cat("Xây OOF predictions qua", K, "folds...\n")

for (k in 1:K) {
  cat(sprintf("  Fold %d/%d\n", k, K))

  val_idx  <- which(fold_id == k)
  tr_fold  <- g_train[-val_idx, ]
  val_fold <- g_train[val_idx,  ]

  # --- Base learner 1: Logistic Regression ---
  bl1 <- glm(target ~ ., data = tr_fold, family = binomial())
  oof_preds[val_idx, "p_lr"] <- predict(bl1, val_fold,
                                          type = "response")

  # --- Base learner 2: Random Forest ---
  bl2 <- ranger(target ~ ., data = tr_fold, num.trees = 200,
                probability = TRUE, verbose = FALSE)
  oof_preds[val_idx, "p_rf"] <- predict(bl2, data = val_fold
                                          )$predictions[, "Bad"]

  # --- Base learner 3: XGBoost ---
  X_tr  <- model.matrix(target ~ . - 1, data = tr_fold)
  y_tr  <- as.integer(tr_fold$target == "Bad")
  X_val <- model.matrix(target ~ . - 1, data = val_fold)

  bl3 <- xgb.train(
    params  = list(objective = "binary:logistic", eta = 0.05,
                   max_depth = 3, eval_metric = "auc"),
    data    = xgb.DMatrix(X_tr, label = y_tr),
    nrounds = 100, verbose = 0
  )
  oof_preds[val_idx, "p_xgb"] <- predict(bl3,
                                           xgb.DMatrix(X_val))
}

cat("OOF predictions hoàn thành!\n")

# Kiểm tra OOF AUC của từng base learner
cat("\n--- OOF AUC (ước lượng performance trên training data) ---\n")
for (col in colnames(oof_preds)) {
  a <- auc(roc(g_train$target, oof_preds[, col],
               levels = c("Good","Bad"), quiet = TRUE))
  cat(sprintf("  %s: %.4f\n", col, round(a, 4)))
}
```

**Huấn luyện meta-model trên OOF predictions:**

```r
meta_df_train <- data.frame(
  oof_preds,
  y = g_train$target
)

# Thử các meta-model khác nhau
# Meta 1: Logistic Regression (phổ biến nhất)
meta_lr <- glm(y ~ ., data = meta_df_train, family = binomial())

# Meta 2: Trung bình đơn giản (baseline)
# Không cần train, chỉ average

# Đánh giá cả hai trên test set
# --- Bước cuối: train lại base learners trên TOÀN BỘ g_train ---
cat("\nTrain lại base learners trên toàn bộ training data...\n")

final_lr  <- glm(target ~ ., data = g_train, family = binomial())
final_rf  <- ranger(target ~ ., data = g_train, num.trees = 300,
                    probability = TRUE, verbose = FALSE)

X_full    <- model.matrix(target ~ . - 1, data = g_train)
y_full    <- as.integer(g_train$target == "Bad")
final_xgb <- xgb.train(
  params  = list(objective = "binary:logistic", eta = 0.05,
                 max_depth = 3, eval_metric = "auc"),
  data    = xgb.DMatrix(X_full, label = y_full),
  nrounds = 100, verbose = 0
)

# Dự đoán trên test set
X_test_g  <- model.matrix(target ~ . - 1, data = g_test)

p_lr_te   <- predict(final_lr,  newdata = g_test, type = "response")
p_rf_te   <- predict(final_rf,  data = g_test)$predictions[, "Bad"]
p_xgb_te  <- predict(final_xgb, xgb.DMatrix(X_test_g))

meta_test_g <- data.frame(p_lr = p_lr_te, p_rf = p_rf_te,
                            p_xgb = p_xgb_te)

# Dự đoán qua meta-model
p_stack_te <- predict(meta_lr, newdata = meta_test_g,
                       type = "response")
p_avg_te   <- rowMeans(meta_test_g)

# Bảng kết quả
cat("\n===== AUC trên Test Set (German Credit) =====\n")
cat("Logistic Regression:  ", get_auc(p_lr_te,    g_test$target), "\n")
cat("Random Forest:        ", get_auc(p_rf_te,    g_test$target), "\n")
cat("XGBoost:              ", get_auc(p_xgb_te,   g_test$target), "\n")
cat("Simple Average:       ", get_auc(p_avg_te,   g_test$target), "\n")
cat("OOF Stacking (LR meta):", get_auc(p_stack_te, g_test$target), "\n")
```

---

### 6.3. Stacking với `caretEnsemble` — Titanic

`caretEnsemble` là cách dùng Stacking trong hệ sinh thái `caret`, nhất quán với Bài 17–20:

```r
library(caretEnsemble)

titanic_f <- titanic %>%
  select(survived, pclass, sex, age, sibsp, parch, fare) %>%
  mutate(survived = factor(survived, levels = c(0,1),
                           labels = c("No","Yes"))) %>%
  drop_na()

set.seed(42)
tit_split <- initial_split(titanic_f, prop = 0.8, strata = survived)
tit_train <- training(tit_split)
tit_test  <- testing(tit_split)

# QUAN TRỌNG: Tất cả base learners PHẢI dùng cùng CV folds
# để OOF predictions nhất quán
ctrl_stack <- trainControl(
  method          = "cv",
  number          = 5,
  savePredictions = "final",   # Bắt buộc để caretEnsemble lấy OOF preds
  classProbs      = TRUE,
  summaryFunction = twoClassSummary,
  index           = createFolds(tit_train$survived, k = 5)  # Cùng folds!
)

# Danh sách base learners
set.seed(42)
base_models <- caretList(
  survived ~ .,
  data       = tit_train,
  trControl  = ctrl_stack,
  metric     = "ROC",
  methodList = c("glm", "rpart", "rf"),
  tuneList   = list(
    xgb = caretModelSpec(
      method   = "xgbTree",
      tuneGrid = expand.grid(
        nrounds = 100, eta = 0.05, max_depth = 3,
        gamma = 0, colsample_bytree = 0.8,
        min_child_weight = 1, subsample = 0.8
      )
    ),
    knn = caretModelSpec(
      method   = "knn",
      tuneGrid = data.frame(k = 7)
    )
  )
)

# Xem AUC của từng base learner qua CV
cat("=== AUC từng base learner (5-Fold CV) ===\n")
for (nm in names(base_models)) {
  auc_cv <- max(base_models[[nm]]$results$ROC, na.rm = TRUE)
  cat(sprintf("  %-12s: %.4f\n", nm, round(auc_cv, 4)))
}

# Kiểm tra tương quan giữa các base learners
modelCor(resamples(base_models))
```

**Xây meta-model:**

```r
# Meta-model: Logistic Regression (mặc định, đơn giản)
set.seed(42)
stack_lr <- caretStack(
  base_models,
  method    = "glm",
  metric    = "ROC",
  trControl = trainControl(
    method          = "cv",
    number          = 5,
    classProbs      = TRUE,
    summaryFunction = twoClassSummary
  )
)

cat("\n--- Hệ số Meta-Model (Logistic Regression) ---\n")
print(coef(stack_lr$ens_model$finalModel))

# Thử meta-model phức tạp hơn: Random Forest
set.seed(42)
stack_rf <- caretStack(
  base_models,
  method    = "rf",
  metric    = "ROC",
  trControl = trainControl(
    method = "cv", number = 5,
    classProbs = TRUE, summaryFunction = twoClassSummary
  ),
  tuneGrid = data.frame(mtry = c(2, 3, 4))
)
```

**Đánh giá trên test set:**

```r
p_stack_lr <- predict(stack_lr, newdata = tit_test, type = "prob")[, "Yes"]
p_stack_rf <- predict(stack_rf, newdata = tit_test, type = "prob")[, "Yes"]

# So sánh tất cả
cat("\n===== AUC trên Test Set (Titanic) =====\n")
for (nm in names(base_models)) {
  p <- predict(base_models[[nm]], newdata = tit_test, type = "prob")[, "Yes"]
  cat(sprintf("  %-20s: %.4f\n", nm, get_auc(p, tit_test$survived)))
}
cat(sprintf("  %-20s: %.4f\n", "Stack (LR meta)",
            get_auc(p_stack_lr, tit_test$survived)))
cat(sprintf("  %-20s: %.4f\n", "Stack (RF meta)",
            get_auc(p_stack_rf, tit_test$survived)))
```

---

### 6.4. Stacking với `stacks` (tidymodels) — Wines

`stacks` là package chính thức của tidymodels cho Stacking, tích hợp hoàn toàn với workflow của Bài 17–20:

```r
library(stacks)

wines <- read.csv("wines.csv", stringsAsFactors = TRUE)
wines_clf <- wines %>%
  select(-quality) %>%
  mutate(type = factor(type))

set.seed(42)
w_split <- initial_split(wines_clf, prop = 0.8, strata = type)
w_train <- training(w_split)
w_test  <- testing(w_split)

# QUAN TRỌNG: Dùng cùng CV folds cho tất cả mô hình
set.seed(42)
w_folds <- vfold_cv(w_train, v = 5, strata = type)

# Recipe chung
wines_recipe <- recipe(type ~ ., data = w_train) %>%
  step_normalize(all_numeric_predictors()) %>%
  step_zv(all_predictors())

# Control: BẮT BUỘC có save_pred = TRUE và save_workflow = TRUE
ctrl_res <- control_resamples(save_pred = TRUE, save_workflow = TRUE)
ctrl_grd <- control_grid(save_pred = TRUE, save_workflow = TRUE)

# --- Base Learner 1: Logistic Regression ---
lr_spec <- logistic_reg(penalty = tune(), mixture = 1) %>%
  set_engine("glmnet") %>%
  set_mode("classification")

lr_wf <- workflow() %>%
  add_recipe(wines_recipe) %>%
  add_model(lr_spec)

set.seed(42)
lr_res <- tune_grid(
  lr_wf,
  resamples = w_folds,
  grid      = grid_regular(penalty(), levels = 5),
  metrics   = metric_set(roc_auc),
  control   = ctrl_grd
)

# --- Base Learner 2: Random Forest ---
rf_spec <- rand_forest(mtry = tune(), trees = 200, min_n = tune()) %>%
  set_engine("ranger") %>%
  set_mode("classification")

rf_wf <- workflow() %>%
  add_recipe(wines_recipe) %>%
  add_model(rf_spec)

set.seed(42)
rf_res <- tune_grid(
  rf_wf,
  resamples = w_folds,
  grid      = grid_regular(mtry(range = c(2,6)),
                            min_n(range = c(2,10)), levels = 3),
  metrics   = metric_set(roc_auc),
  control   = ctrl_grd
)

# --- Base Learner 3: XGBoost ---
xgb_spec <- boost_tree(trees = tune(), learn_rate = tune(),
                        tree_depth = tune()) %>%
  set_engine("xgboost") %>%
  set_mode("classification")

xgb_wf <- workflow() %>%
  add_recipe(wines_recipe) %>%
  add_model(xgb_spec)

set.seed(42)
xgb_res <- tune_grid(
  xgb_wf,
  resamples = w_folds,
  grid      = grid_regular(trees(range = c(50, 300)),
                            learn_rate(range = c(-2, -1)),
                            tree_depth(range = c(2, 5)), levels = 3),
  metrics   = metric_set(roc_auc),
  control   = ctrl_grd
)

# --- Base Learner 4: KNN ---
knn_spec <- nearest_neighbor(neighbors = tune()) %>%
  set_engine("kknn") %>%
  set_mode("classification")

knn_wf <- workflow() %>%
  add_recipe(wines_recipe) %>%
  add_model(knn_spec)

set.seed(42)
knn_res <- tune_grid(
  knn_wf,
  resamples = w_folds,
  grid      = data.frame(neighbors = c(3, 5, 7, 11, 15)),
  metrics   = metric_set(roc_auc),
  control   = ctrl_grd
)
```

**Xây Stack:**

```r
# Khởi tạo stack
wines_stack <- stacks() %>%
  add_candidates(lr_res)  %>%   # Thêm tất cả candidates từ LR
  add_candidates(rf_res)  %>%   # (mỗi tham số là một candidate)
  add_candidates(xgb_res) %>%
  add_candidates(knn_res)

cat("Số candidates:", nrow(collect_parameters(wines_stack)), "\n")

# Blend: chọn candidates tốt nhất và học trọng số meta-model
# penalty: regularization của LASSO meta-model (loại candidates xấu)
set.seed(42)
wines_blend <- blend_predictions(
  wines_stack,
  penalty   = 10^seq(-3, -0.5, length.out = 10),
  metrics   = metric_set(roc_auc)
)

# Xem kết quả blending
autoplot(wines_blend) +
  labs(title = "Stacking — Regularization path (Wines)")

autoplot(wines_blend, type = "weights") +
  labs(title = "Trọng số của từng candidate trong Stack")

# Candidates được chọn (trọng số > 0)
collect_parameters(wines_blend) %>%
  filter(coef != 0) %>%
  arrange(desc(coef)) %>%
  print()
```

**Fit lại base learners trên toàn training data:**

```r
# Bước cuối: fit lại trên toàn bộ training data
wines_final <- fit_members(wines_blend)

# Đánh giá trên test set
pred_stack <- predict(wines_final, w_test, type = "prob")$.pred_red

roc_stack <- roc(w_test$type, pred_stack,
                 levels = c("white","red"), quiet = TRUE)
cat("AUC — Stacking (wines):", round(auc(roc_stack), 4), "\n")

# So sánh với từng base learner riêng lẻ
best_lr  <- fit(finalize_workflow(lr_wf,  select_best(lr_res,  "roc_auc")),
                w_train)
best_rf  <- fit(finalize_workflow(rf_wf,  select_best(rf_res,  "roc_auc")),
                w_train)
best_xgb <- fit(finalize_workflow(xgb_wf, select_best(xgb_res, "roc_auc")),
                w_train)
best_knn <- fit(finalize_workflow(knn_wf, select_best(knn_res, "roc_auc")),
                w_train)

get_auc_wf <- function(fitted_wf, test, truth, pos) {
  p <- predict(fitted_wf, test, type = "prob")[[paste0(".pred_", pos)]]
  round(auc(roc(test[[truth]], p,
               levels = levels(test[[truth]]), quiet = TRUE)), 4)
}

cat("\n===== AUC trên Test Set (Wines) =====\n")
cat("Logistic Regression:", get_auc_wf(best_lr,  w_test, "type", "red"), "\n")
cat("Random Forest:      ", get_auc_wf(best_rf,  w_test, "type", "red"), "\n")
cat("XGBoost:            ", get_auc_wf(best_xgb, w_test, "type", "red"), "\n")
cat("KNN:                ", get_auc_wf(best_knn, w_test, "type", "red"), "\n")
cat("----------------------------\n")
cat("Stacking:           ", round(auc(roc_stack), 4), "\n")
```

---

### 6.5. Stacking cho hồi quy — Wines Quality

Stacking không chỉ dùng cho phân loại — áp dụng tốt cho cả hồi quy:

```r
wines_reg <- wines %>% select(-type)

set.seed(42)
wr_split  <- initial_split(wines_reg, prop = 0.8)
wr_train  <- training(wr_split)
wr_test   <- testing(wr_split)
wr_folds  <- vfold_cv(wr_train, v = 5)

reg_recipe <- recipe(quality ~ ., data = wr_train) %>%
  step_normalize(all_numeric_predictors())

ctrl_reg <- control_grid(save_pred = TRUE, save_workflow = TRUE)

# Base learners cho hồi quy
lm_spec <- linear_reg(penalty = tune(), mixture = 1) %>%
  set_engine("glmnet")

rf_reg_spec <- rand_forest(mtry = tune(), trees = 200) %>%
  set_engine("ranger") %>%
  set_mode("regression")

xgb_reg_spec <- boost_tree(trees = tune(), learn_rate = tune()) %>%
  set_engine("xgboost") %>%
  set_mode("regression")

# Workflow + tune từng model
make_wf <- function(spec, recipe) {
  workflow() %>% add_recipe(recipe) %>% add_model(spec)
}

set.seed(42)
lm_res_r <- tune_grid(
  make_wf(lm_spec, reg_recipe), resamples = wr_folds,
  grid = grid_regular(penalty(), levels = 5),
  metrics = metric_set(rmse, rsq), control = ctrl_reg
)

rf_res_r <- tune_grid(
  make_wf(rf_reg_spec, reg_recipe), resamples = wr_folds,
  grid = grid_regular(mtry(range = c(2,6)), levels = 4),
  metrics = metric_set(rmse, rsq), control = ctrl_reg
)

xgb_res_r <- tune_grid(
  make_wf(xgb_reg_spec, reg_recipe), resamples = wr_folds,
  grid = grid_regular(trees(range = c(50,300)),
                       learn_rate(range = c(-2,-1)), levels = 3),
  metrics = metric_set(rmse, rsq), control = ctrl_reg
)

# Stack hồi quy
reg_stack <- stacks() %>%
  add_candidates(lm_res_r)  %>%
  add_candidates(rf_res_r)  %>%
  add_candidates(xgb_res_r)

set.seed(42)
reg_blend <- blend_predictions(reg_stack,
                                 penalty = 10^seq(-3, -0.5, length.out = 8),
                                 metrics = metric_set(rmse))
reg_final <- fit_members(reg_blend)

# Đánh giá
pred_reg_stack <- predict(reg_final, wr_test)$.pred
rmse_stack <- sqrt(mean((wr_test$quality - pred_reg_stack)^2))
r2_stack   <- 1 - sum((wr_test$quality - pred_reg_stack)^2) /
                  sum((wr_test$quality - mean(wr_test$quality))^2)

cat("===== Hồi quy Wine Quality =====\n")
cat(sprintf("Stacking — RMSE: %.4f | R²: %.4f\n", rmse_stack, r2_stack))
```

---

### 6.6. So sánh toàn diện Bài 17–21 — Medical Care

```r
medical <- read.csv("medical_care.csv", stringsAsFactors = TRUE)
medical_clean <- medical %>%
  select(UCURNINS, UMARSTAT, USATMED, REGION, FHOSP, FDENT, FEMER,
         FDOCT, UIMMSTAT, UAGE, U_FTPT, U_WKSLY, UBRACE, GENDER, UEDUC3) %>%
  drop_na() %>%
  mutate(UCURNINS = factor(UCURNINS))

set.seed(42)
med_split <- initial_split(medical_clean, prop = 0.8, strata = UCURNINS)
med_train <- training(med_split)
med_test  <- testing(med_split)
med_folds <- vfold_cv(med_train, v = 5, strata = UCURNINS)

med_recipe <- recipe(UCURNINS ~ ., data = med_train) %>%
  step_dummy(all_nominal_predictors()) %>%
  step_zv(all_predictors())

ctrl_med <- control_grid(save_pred = TRUE, save_workflow = TRUE)

# Base learners
lr_med_spec <- logistic_reg() %>% set_engine("glm")

rf_med_spec <- rand_forest(mtry = tune(), trees = 200) %>%
  set_engine("ranger") %>% set_mode("classification")

xgb_med_spec <- boost_tree(trees = 200, learn_rate = 0.05,
                             tree_depth = 4) %>%
  set_engine("xgboost") %>% set_mode("classification")

# Fit từng model
set.seed(42)
lr_med_res <- fit_resamples(
  workflow() %>% add_recipe(med_recipe) %>% add_model(lr_med_spec),
  resamples = med_folds,
  metrics   = metric_set(roc_auc),
  control   = control_resamples(save_pred = TRUE, save_workflow = TRUE)
)

rf_med_res <- tune_grid(
  workflow() %>% add_recipe(med_recipe) %>% add_model(rf_med_spec),
  resamples = med_folds,
  grid      = data.frame(mtry = c(3, 5, 7)),
  metrics   = metric_set(roc_auc),
  control   = ctrl_med
)

xgb_med_res <- fit_resamples(
  workflow() %>% add_recipe(med_recipe) %>% add_model(xgb_med_spec),
  resamples = med_folds,
  metrics   = metric_set(roc_auc),
  control   = control_resamples(save_pred = TRUE, save_workflow = TRUE)
)

# Stack
med_stack <- stacks() %>%
  add_candidates(lr_med_res)  %>%
  add_candidates(rf_med_res)  %>%
  add_candidates(xgb_med_res)

set.seed(42)
med_blend <- blend_predictions(med_stack,
                                 penalty = 10^seq(-3, -0.5, length.out = 8),
                                 metrics = metric_set(roc_auc))
med_final <- fit_members(med_blend)

# Đánh giá tất cả
pred_med_stack <- predict(med_final, med_test, type = "prob")$.pred_Yes
roc_med_stack  <- roc(med_test$UCURNINS, pred_med_stack,
                       levels = c("No","Yes"), quiet = TRUE)

cat("\n========================================\n")
cat("  TỔNG KẾT BÀI 17–21: Medical Care\n")
cat("========================================\n")
cat("Bài 17 — Logistic Regression: (xem lại Bài 17)\n")
cat("Bài 18 — Decision Tree:       (xem lại Bài 18)\n")
cat("Bài 19 — Random Forest:       (xem lại Bài 19)\n")
cat("Bài 20 — XGBoost:             (xem lại Bài 20)\n")
cat(sprintf("Bài 21 — Stacking:            %.4f\n",
            round(auc(roc_med_stack), 4)))
```

---

### 6.7. Visualize và phân tích kết quả Stacking

```r
# Vẽ biểu đồ so sánh AUC của tất cả mô hình trên German Credit
results_summary <- tibble(
  Model = c("Logistic Reg\n(Bài 17)", "Decision Tree\n(Bài 18)",
            "Random Forest\n(Bài 19)", "XGBoost\n(Bài 20)",
            "Simple Average", "OOF Stacking\n(Bài 21)"),
  AUC   = c(
    get_auc(p_lr_te,    g_test$target),
    get_auc(predict(rpart(target~., g_train, method="class"),
                    g_test, type="prob")[,"Bad"], g_test$target),
    get_auc(p_rf_te,    g_test$target),
    get_auc(p_xgb_te,   g_test$target),
    get_auc(p_avg_te,   g_test$target),
    get_auc(p_stack_te, g_test$target)
  ),
  Group = c("Single Model", "Single Model", "Ensemble",
            "Ensemble", "Ensemble", "Ensemble")
)

ggplot(results_summary,
       aes(x = reorder(Model, AUC), y = AUC, fill = Group)) +
  geom_col(width = 0.6) +
  geom_text(aes(label = round(AUC, 4)), hjust = -0.1, size = 3.5) +
  coord_flip(ylim = c(min(results_summary$AUC) - 0.02, 1.0)) +
  scale_fill_manual(values = c("Single Model" = "steelblue",
                                "Ensemble"     = "tomato")) +
  labs(title    = "So sánh AUC — Bài 17 đến Bài 21 (German Credit)",
       subtitle  = "Đỏ = Ensemble methods, Xanh = Single models",
       x = NULL, y = "AUC (Test Set)", fill = NULL) +
  theme_minimal() +
  theme(legend.position = "bottom")
```

---

## 7. Những điều cần nhớ khi dùng Stacking

### 7.1. Chọn base learners đa dạng

```r
# Kiểm tra tương quan giữa OOF predictions
# Muốn: tương quan thấp (< 0.9) → các mô hình học khác nhau
cor_matrix <- cor(oof_preds)
corrplot::corrplot(cor_matrix, method = "number",
                   title = "Tương quan OOF Predictions")
```

**Nguyên tắc chọn base learners:**
- Kết hợp mô hình **tuyến tính** (Logistic/Linear Regression) và **phi tuyến** (Tree-based).
- Kết hợp mô hình **nhiều biến** (Linear Reg) và **ít biến mỗi lần** (Random Forest).
- Kết hợp mô hình **đơn giản** (KNN) và **phức tạp** (XGBoost).

### 7.2. Chọn meta-learner phù hợp

| Meta-learner | Ưu điểm | Khi nào dùng |
|---|---|---|
| Logistic Regression | Đơn giản, giải thích được | Mặc định, ít overfit |
| Ridge Regression | Tự động regularize | Khi nhiều base learners |
| Random Forest | Phi tuyến, robust | Khi base learners tương quan cao |
| Linear (LASSO) | Tự loại candidates | Khi dùng `stacks` package |

### 7.3. Những lỗi thường gặp

```
❌ Lỗi 1: Dùng cùng training data cho cả base learner và meta-learner
   → Rò rỉ thông tin, đánh giá quá lạc quan
   ✅ Sửa: Dùng OOF predictions hoặc Blending

❌ Lỗi 2: Base learners quá giống nhau
   → Meta-model không học được gì thêm
   ✅ Sửa: Kết hợp các loại mô hình đa dạng

❌ Lỗi 3: Meta-model quá phức tạp
   → Overfit trên OOF predictions
   ✅ Sửa: Dùng Logistic Regression hoặc Ridge làm meta-model

❌ Lỗi 4: Quên train lại base learners trên toàn bộ training data
   → Inference kém vì base learners chỉ thấy K-1/K dữ liệu
   ✅ Sửa: Luôn refit trên full training data trước khi predict test
```

---

## 8. Tổng kết toàn bộ chuỗi Bài 17–21

```
BÀI 17: Cross-Validation
  └─ Đánh giá mô hình khách quan, tránh overfitting

BÀI 18: Decision Tree
  └─ Mô hình cơ sở, dễ hiểu, variance cao

BÀI 19: Bagging & Random Forest
  └─ Nhiều cây song song → Giảm Variance
  └─ OOB Error: đánh giá không cần tập test riêng

BÀI 20: Boosting (AdaBoost, GBM, XGBoost)
  └─ Nhiều cây tuần tự → Giảm Bias
  └─ Early Stopping: kiểm soát overfitting

BÀI 21: Stacking
  └─ Kết hợp mô hình khác loại → Tận dụng điểm mạnh từng loại
  └─ OOF Predictions: tránh rò rỉ thông tin

                    Hiệu năng tăng dần →
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│Log. Reg  │  D. Tree │  Bagging │ Boosting │ Stacking │
│ (B.17)   │  (B.18)  │  (B.19)  │  (B.20)  │  (B.21)  │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│Dễ giải   │Dễ giải   │Khó giải  │Khó giải  │Khó giải  │
│thích nhất│thích     │thích     │thích     │thích nhất│
├──────────┼──────────┼──────────┼──────────┼──────────┤
│Ít tham số│Ít tham số│Vừa       │Nhiều     │Nhiều nhất│
│tune      │tune      │          │tham số   │tham số   │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

**Lời khuyên thực tế:**

- Bắt đầu với **Logistic Regression** làm baseline.
- Nếu cần cải thiện → thử **Random Forest** trước (ít tuning, OOB error tiện lợi).
- Nếu cần tốt hơn nữa → **XGBoost** với early stopping.
- Nếu muốn vắt kiệt hiệu năng → **Stacking** (nhưng cần thêm công sức).
- Luôn kiểm tra xem sự cải thiện có **đáng kể về mặt thực tế** không, không chỉ về số học.

---

## 9. Bài tập

### Bài tập 1 — Titanic (`titanic.csv`) — Blending tự tay

1. Chia dữ liệu thành 3 phần: 60% base train, 20% blend, 20% test.
2. Huấn luyện 4 base learners (Logistic Regression, Decision Tree, Random Forest, XGBoost) trên phần base train.
3. Tạo meta-features từ dự đoán trên blend set. In ra ma trận tương quan — các mô hình có đủ đa dạng không?
4. Huấn luyện meta-model (Logistic Regression) và đánh giá AUC trên test set. So sánh với từng base learner riêng lẻ.
5. *(Nâng cao)* Thử thêm KNN và SVM làm base learner. AUC stacking có tăng không? Kiểm tra tương quan OOF predictions.

### Bài tập 2 — German Credit (`german1.csv`) — OOF Stacking

1. Xây OOF Stacking với 5-Fold CV cho 3 base learners (LR, RF, XGBoost). In ra OOF AUC của từng mô hình.
2. Huấn luyện meta-model Logistic Regression trên OOF predictions. Hệ số nào lớn nhất — mô hình nào được "tin tưởng" nhất?
3. Thử meta-model là **trung bình đơn giản** (simple average). So sánh với meta Logistic Regression — cái nào tốt hơn?
4. So sánh AUC của: LR, RF, XGBoost, Simple Average, OOF Stacking trên test set bằng biểu đồ cột.
5. *(Nâng cao)* Thử thêm biến gốc vào meta-features (bên cạnh OOF predictions). AUC có cải thiện không? Có nguy cơ gì?

### Bài tập 3 — Wines (`wines.csv`) — Stacking với `stacks`

1. Dùng package `stacks` để xây Stacking phân loại `type` với 4 base learners. Xem candidates nào được chọn (trọng số > 0).
2. Vẽ `autoplot(blend)` — regularization path. Giải thích: penalty lớn hơn ảnh hưởng đến số candidates như thế nào?
3. Xây Stacking hồi quy để dự đoán `quality`. So sánh RMSE với: Linear Regression, Random Forest, XGBoost.
4. *(Nâng cao)* Thêm biến `alcohol_density = alcohol / density` vào recipe. Biến mới có vào top candidates không?

### Bài tập 4 — Medical Care (`medical_care.csv`) — Tổng kết

1. Xây Stacking với 3 base learners (LR, RF, XGBoost) dùng `stacks`. Đánh giá AUC trên test set.
2. Tạo bảng tổng kết AUC của **tất cả mô hình từ Bài 17 đến Bài 21** trên cùng test set của Medical Care.
3. Vẽ biểu đồ cột so sánh AUC theo thứ tự tăng dần. Sự cải thiện từ mỗi bài có đáng kể không?
4. *(Nâng cao)* Đo thời gian huấn luyện và inference của từng phương pháp. Tính **AUC/giây** — phương pháp nào hiệu quả nhất về mặt tính toán?

---

## 10. Tài liệu tham khảo

- **stacks package:** <https://stacks.tidymodels.org/>
- **caretEnsemble package:** <https://cran.r-project.org/package=caretEnsemble>
- Wolpert, D. H. (1992). Stacked generalization. *Neural Networks*, 5(2), 241–259. *(Bài báo gốc về Stacking)*
- Breiman, L. (1996). Stacked regressions. *Machine Learning*, 24(1), 49–64.
- James, G. et al. (2021). *An Introduction to Statistical Learning* (2nd ed.). Springer. Ch. 8.
- Hastie, T. et al. (2009). *The Elements of Statistical Learning* (2nd ed.). Springer. Ch. 8.8: Stacking.
- **tidymodels:** <https://www.tidymodels.org/>

---

*Bài giảng được soạn cho môn Data Science với R — Bài 21: Stacking & Ensemble Tổng Quát*  
*Hoàn thành chuỗi Bài 17–21: Cross-Validation → Decision Tree → Bagging/RF → Boosting → Stacking*
